import React,{useEffect,useRef,useState}from'react';
import{createRoot}from'react-dom/client';
import type{Word,State,Screen,ChallengeState,Status,ChallengeKind}from'./types';
import{loadState,saveState,resetState,defaultProgress,validateBackup}from'./storage';
import{dailySet,buildChallenge,recordAnswer,stats,dueWords,localToday}from'./engine';
import{updateGamification}from'./achievement';
import LevelCard from'./components/LevelCard';import Achievements from'./components/Achievements';import GoalWidget from'./components/GoalWidget';import BadgePanel from'./components/BadgePanel';import'./styles.css';

type Toast={icon:string;title:string;text:string};
const App=()=>{const[baseWords,setBaseWords]=useState<Word[]>([]),[state,setState]=useState<State>(loadState),[screen,setScreen]=useState<Screen>('home'),[selected,setSelected]=useState<string|null>(null),[detail,setDetail]=useState<number>(),[toast,setToast]=useState<Toast|null>(null);const previous=useRef<State|null>(null);
 useEffect(()=>{fetch('./words.json').then(r=>r.json()).then(setBaseWords)},[]);useEffect(()=>saveState(state),[state]);useEffect(()=>{navigator.serviceWorker?.register('./sw.js')},[]);
 const words=[...baseWords,...state.customWords.filter(c=>!baseWords.some(w=>w.id===c.id))];
 useEffect(()=>{if(!baseWords.length)return;setState(s=>{const now=localToday();if(s.activeDate===now)return s;const elapsed=Math.max(1,Math.floor((new Date(now).getTime()-new Date(s.activeDate).getTime())/86400000));return{...s,activeDate:now,learningDay:s.learningDay+elapsed,todayIds:s.previewIds.length?s.previewIds:[],previewIds:[],todayCursor:0,todayCompleted:false,todayChallengeTarget:0,todayChallengeCompleted:0,challenge:undefined}})},[baseWords.length]);
 useEffect(()=>{if(words.length&&!state.todayIds.length){const set=dailySet(words,state.settings.dailyWords,state.learningDay,state.learned);setState(s=>({...s,todayIds:set.map(w=>w.id),todayCursor:0,todayCompleted:false}))}},[words.length,state.todayIds.length,state.learningDay]);
 useEffect(()=>{if(!previous.current){previous.current=state;return}const p=previous.current,newA=state.achievements.find(a=>a.unlocked&&!p.achievements.find(x=>x.id===a.id)?.unlocked);if(state.level>p.level)setToast({icon:'⬆️',title:'LEVEL UP',text:`Level ${state.level}`});else if(newA)setToast({icon:'🏅',title:'Achievement Unlocked',text:newA.title});previous.current=state},[state]);useEffect(()=>{if(!toast)return;const t=setTimeout(()=>setToast(null),3200);return()=>clearTimeout(t)},[toast]);
 if(!words.length)return <div className="splash"><div className="logo">W</div><b>WordLock yükleniyor…</b></div>;
 const today=state.todayIds.map(id=>words.find(w=>w.id===id)).filter(Boolean)as Word[],tomorrow=state.previewIds.map(id=>words.find(w=>w.id===id)).filter(Boolean)as Word[],st=stats(state),due=dueWords(words,state),list=(ids:number[])=>ids.map(id=>words.find(w=>w.id===id)).filter(Boolean)as Word[];
 const toggle=(id:number)=>setState(s=>{const favorites=s.favorites.includes(id)?s.favorites.filter(x=>x!==id):[...s.favorites,id];return updateGamification({...s,favorites})});const open=(id:number)=>{setDetail(id);setScreen('word')};
 const preview=()=>{setState(s=>{if(s.previewIds.length)return s;const used=[...new Set([...s.learned,...s.todayIds])],set=dailySet(words,s.settings.dailyWords,s.learningDay+1,used);return{...s,previewIds:set.map(w=>w.id)}});setScreen('tomorrow')};
 const start=(kind:ChallengeKind)=>{const questions=buildChallenge(kind,words,state);if(!questions.length)return;const preStatuses=Object.fromEntries(questions.map(q=>[q.wordId,state.progress[q.wordId]?.status||'new']))as Record<number,Status>;setState(s=>({...s,todayChallengeTarget:kind==='daily'?questions.length:s.todayChallengeTarget,challenge:{kind,questions,index:0,answers:[],score:0,bestRun:0,currentRun:0,preStatuses}}));setSelected(null);setScreen('challenge')};
 return <div className="app"><Header go={setScreen}/>{screen==='home'&&<Home s={state} st={st} today={today} due={due.length} go={setScreen} start={start} preview={preview}/>} {screen==='today'&&<Today words={today} s={state} setS={setState} toggle={toggle} back={()=>setScreen('home')} start={()=>start('daily')}/>} {screen==='tomorrow'&&<Tomorrow words={tomorrow} back={()=>setScreen('home')}/>} {screen==='challenge'&&state.challenge&&<Challenge s={state} setS={setState} words={words} selected={selected} setSelected={setSelected} done={()=>setScreen('result')}/>} {screen==='result'&&<Result s={state} go={setScreen} start={start}/>} {screen==='learned'&&<WordList title="Öğrenilen Kelimeler" words={list(state.learned)} s={state} open={open} back={()=>setScreen('home')}/>} {screen==='favorites'&&<WordList title="Favoriler" words={list(state.favorites)} s={state} open={open} back={()=>setScreen('home')} action={()=>start('favorites')} actionText="Favori Challenge Başlat"/>} {screen==='risky'&&<WordList title="Riskli Kelimeler" words={words.filter(w=>state.progress[w.id]?.status==='risky')} s={state} open={open} back={()=>setScreen('home')} action={()=>start('risky')} actionText="Risk Challenge Başlat"/>} {screen==='master'&&<WordList title="Master Kelimeler" words={words.filter(w=>['master','masterReview'].includes(state.progress[w.id]?.status))} s={state} open={open} back={()=>setScreen('home')} action={()=>start('master')} actionText="Master Challenge Başlat"/>} {screen==='all'&&<AllWords words={words} s={state} open={open} back={()=>setScreen('home')}/>} {screen==='review'&&<WordList title="Bugünkü Tekrarlar" words={due} s={state} open={open} back={()=>setScreen('home')} action={()=>start('review')} actionText="Tekrar Challenge Başlat"/>} {screen==='achievements'&&<Achievements state={state} back={()=>setScreen('home')}/>} {screen==='custom'&&<CustomWords s={state} setS={setState} back={()=>setScreen('settings')}/>} {screen==='settings'&&<Settings s={state} setS={setState} back={()=>setScreen('home')} custom={()=>setScreen('custom')}/>} {screen==='word'&&detail&&<WordDetail w={words.find(w=>w.id===detail)!} s={state} toggle={toggle} back={()=>setScreen('home')}/>}<Nav current={screen} go={setScreen}/>{toast&&<div className="toast"><b>{toast.icon} {toast.title}</b><span>{toast.text}</span></div>}</div>};
const Header=({go}:any)=><header><button className="brand" onClick={()=>go('home')}><span>WORDLOCK</span><b>V4B</b></button><button className="gear" onClick={()=>go('settings')}>⚙</button></header>;
function Home({s,st,today,due,go,start,preview}:any){return <main className="page"><section className="welcome"><p>ÖĞRENME GÜNÜ {s.learningDay}</p><h1>{s.todayCompleted?'Bugünün kelimeleri tamamlandı.':'Bugünkü kelimeler hazır.'}</h1><p className="heroText">{today.length} yeni kelimeyi sırayla çalış, sonra challenge'a geç.</p><button className="heroButton" onClick={()=>go('today')}>{s.todayCompleted?'Kelimeleri Tekrar Gör':'Bugünkü Kelimelere Başla'} →</button></section><section className="gamificationGrid"><LevelCard state={s}/><button className="achievementSummary" onClick={()=>go('achievements')}><small>ACHIEVEMENTS</small><b>🏅 {s.achievements.filter((a:any)=>a.unlocked).length} / {s.achievements.length}</b><span>Tüm başarıları görüntüle →</span></button></section><BadgePanel state={s}/><section className="stats"><button onClick={()=>go('learned')}><span>Öğrenilen</span><b>{s.learned.length}<small>/{384+s.customWords.length}</small></b></button><button onClick={()=>go('master')}><span>Master</span><b>{st.master}</b></button><button onClick={()=>go('risky')}><span>Riskli</span><b>{st.risky}</b></button><button onClick={()=>go('favorites')}><span>Favoriler</span><b>{s.favorites.length}</b></button></section><section className="intelligence"><button onClick={()=>go('all')}><small>KELİME KÜTÜPHANESİ</small><b>Tüm Kelimeler</b><span>Kütüphaneyi görüntüle →</span></button><button onClick={()=>due&&go('review')}><small>BUGÜNKÜ TEKRAR</small><b>{due} Kelime Due</b><span>{due?'Planlı tekrarları görüntüle →':'Bugün tekrar yok'}</span></button></section><GoalWidget state={s}/><section className="featureGrid"><button onClick={()=>start('practice')}><small>EKSTRA ÇALIŞMA</small><b>Bugünün Kelimelerini Pekiştir</b><span>XP yok, öğrenme ilerlemesi güncellenir →</span></button><button onClick={preview}><small>YARINI ÖN İZLE</small><b>Sonraki Günün Kelimeleri</b><span>Sadece ön izleme, ilerleme ve XP yok →</span></button></section><section className="panel todayPanel"><div className="sectionTitle"><div><small>BUGÜNÜN SETİ</small><h2>{s.todayCompleted?'Tamamlandı':'Devam ediyor'}</h2></div><b>{Math.min(s.todayCursor+1,today.length)}/{today.length}</b></div><div className="wordPills">{today.map((w:Word,i:number)=><span className={i<=s.todayCursor?'seen':''} key={w.id}>{w.word}</span>)}</div><div className="homeActions"><button className="secondary" onClick={()=>go('today')}>{s.todayCompleted?'Kelimeleri Yeniden İncele':'Kaldığın Yerden Devam Et'}</button>{s.todayCompleted&&<button className="primary" onClick={()=>start('daily')}>Challenge'a Geç</button>}</div></section></main>}
function Today({words,s,setS,toggle,back,start}:any){const i=Math.min(s.todayCursor,words.length-1),w=words[i];if(!w)return null;const move=(n:number)=>setS((x:State)=>({...x,todayCursor:n,learned:[...new Set([...x.learned,...words.slice(0,n+1).map((a:Word)=>a.id)])]}));return <main className="page"><div className="topline"><button className="back" onClick={back}>← Ana Sayfa</button><b>{i+1}/{words.length}</b></div><div className="progress"><i style={{width:`${(i+1)/words.length*100}%`}}/></div><WordCard w={w} fav={s.favorites.includes(w.id)} toggle={toggle}/><div className="actions"><button className="secondary" disabled={!i} onClick={()=>move(i-1)}>← Önceki</button>{i===words.length-1?<button className="primary" onClick={()=>{setS((x:State)=>updateGamification({...x,todayCompleted:true,learned:[...new Set([...x.learned,...words.map((a:Word)=>a.id)])]}));start()}}>Challenge'a Başla</button>:<button className="primary" onClick={()=>move(i+1)}>Öğrendim, Sonraki →</button>}</div></main>}
function Tomorrow({words,back}:any){return <main className="page"><button className="back" onClick={back}>← Ana Sayfa</button><div className="title"><h1>Yarının Kelimeleri</h1><p>Ön izleme. Bu ekranda ilerleme ve XP kaydedilmez.</p></div><div className="previewGrid">{words.map((w:Word)=><article className="previewCard" key={w.id}><span className="pos">{w.pos}</span><h2>{w.word}</h2><p>{w.definition}</p><b>{w.turkish}</b></article>)}</div></main>}
const WordCard=({w,fav,toggle}:any)=><article className="wordCard"><div className="wordHead"><span className="pos">{w.pos}</span><button className={fav?'star on':'star'} onClick={()=>toggle(w.id)}>★</button></div><h1>{w.word}</h1><section><label>ENGLISH DEFINITION</label><p>{w.definition}</p></section><section><label>TÜRKÇE</label><p className="turkish">{w.turkish}</p></section><div className="two"><section><label>SYNONYMS</label><div className="tags">{w.synonyms.map((x:string)=><span key={x}>{x}</span>)}</div></section><section><label>ANTONYMS</label><div className="tags red">{w.antonyms.length?w.antonyms.map((x:string)=><span key={x}>{x}</span>):<span>N/A</span>}</div></section></div><section><label>EXAMPLES</label>{w.examples.map((x:string,k:number)=><p className="example" key={k}><b>{k+1}</b>{x}</p>)}</section></article>;
function Challenge({s,setS,words,selected,setSelected,done}:any){const c=s.challenge as ChallengeState,q=c.questions[c.index],w=words.find((x:Word)=>x.id===q.wordId),practice=c.kind==='practice',isCorrect=selected===q.correctAnswer;const answer=(value:string|null,unknown=false)=>{if(selected!==null)return;const correct=!unknown&&value===q.correctAnswer,next=recordAnswer(s,q.wordId,correct,unknown,q.type),run=correct?c.currentRun+1:0;setS({...next,challenge:{...c,answers:[...c.answers,{wordId:q.wordId,correct,unknown}],score:c.score+(correct&&!practice?10:0),currentRun:run,bestRun:Math.max(c.bestRun,run)}});setSelected(unknown?'__unknown__':value)};const next=()=>{const cc=s.challenge as ChallengeState;if(cc.index<cc.questions.length-1){setS((x:State)=>({...x,challenge:{...x.challenge!,index:x.challenge!.index+1}}));setSelected(null);return}const a=cc.answers,ids=[...new Set(a.map(x=>x.wordId))],newRisky=ids.filter(id=>cc.preStatuses[id]!=='risky'&&s.progress[id]?.status==='risky'),newMaster=ids.filter(id=>cc.preStatuses[id]!=='master'&&s.progress[id]?.status==='master'),daily=cc.kind==='daily',scored=cc.kind!=='practice',bonus=daily?50+(cc.bestRun>=10?40:cc.bestRun>=5?20:0):0,stamp=localToday(),newStreak=daily&&s.lastCompleted!==stamp?s.streak+1:s.streak;setS((x:State)=>updateGamification({...x,xp:x.xp+(scored?cc.score+bonus:0),streak:newStreak,lastCompleted:daily?stamp:x.lastCompleted,totalChallenges:x.totalChallenges+(scored?1:0),totalCorrectAnswers:x.totalCorrectAnswers+(scored?a.filter(y=>y.correct).length:0),todayChallengeCompleted:daily?a.length:x.todayChallengeCompleted,lastResult:{kind:cc.kind,correct:a.filter(y=>y.correct).length,wrong:a.filter(y=>!y.correct&&!y.unknown).length,unknown:a.filter(y=>y.unknown).length,xp:scored?cc.score+bonus:0,total:a.length,date:stamp,newRisky,newMaster}}));done()};return <main className="page"><div className="topline"><b>{practice?'Practice':'Question'} {c.index+1}/{c.questions.length}</b><span>{practice?'No XP':`${c.score} XP`}</span></div><div className="progress"><i style={{width:`${(c.index+1)/c.questions.length*100}%`}}/></div><section className="panel question"><h2 style={{whiteSpace:'pre-line'}}>{q.prompt}</h2>{q.options.map((o:string)=><button key={o} disabled={selected!==null} className={selected!==null?(o===q.correctAnswer?'correct':o===selected?'wrong':''):''} onClick={()=>answer(o)}>{o}</button>)}{selected===null&&<button className="unknown" onClick={()=>answer(null,true)}>I don't know</button>}{selected!==null&&<div className="feedback"><h3>{isCorrect?`✓ Correct${practice?'':', +10 XP'}`:`Correct answer: ${q.correctAnswer}`}</h3>{!isCorrect&&s.settings.showTurkishAfterWrong&&<p><b>Türkçe:</b> {w.turkish}</p>}{!isCorrect&&s.settings.showExamplesAfterWrong&&<p>{w.examples[0]}</p>}<button className="primary" onClick={next}>{c.index===c.questions.length-1?'View Result':'Next Question'}</button></div>}</section></main>}
function Result({s,go,start}:any){const r=s.lastResult;if(!r)return null;return <main className="page"><section className="panel result"><div className="trophy">{r.kind==='practice'?'📚':'🏆'}</div><h1>{r.kind==='practice'?'Ekstra Çalışma Tamamlandı':'Challenge Tamamlandı'}</h1><div className="resultGrid"><div><b>{r.correct}</b><span>Doğru</span></div><div><b>{r.wrong+r.unknown}</b><span>Yanlış/Bilmiyorum</span></div><div><b>{r.xp}</b><span>XP</span></div></div>{r.kind==='practice'&&<p className="explain">İlerleme güncellendi. Ekstra çalışma XP ve challenge sayısına eklenmedi.</p>}<div className="actions"><button className="secondary" onClick={()=>go('home')}>Ana Sayfa</button>{r.kind==='practice'&&<button className="primary" onClick={()=>start('practice')}>Tekrar Çalış</button>}{r.newRisky.length>0&&r.kind!=='practice'&&<button className="primary" onClick={()=>start('risky')}>Riskli Kelimeleri Tekrarla</button>}</div></section></main>}
function WordList({title,words,s,open,back,action,actionText,empty}:any){return <main className="page"><button className="back" onClick={back}>← Ana Sayfa</button><div className="title"><h1>{title}</h1><p>{words.length} kelime</p></div>{!words.length?<section className="panel empty"><h2>Henüz kelime yok</h2><p>{empty||'Bu liste ilerledikçe otomatik oluşacak.'}</p></section>:<><div className="list">{words.map((w:Word)=>{const p=s.progress[w.id]||defaultProgress();return <button key={w.id} onClick={()=>open(w.id)}><div><b>{w.word}</b><small>{w.turkish}</small></div><span>{p.seen?`${p.accuracy}% · ${p.status}`:'Yeni'}</span></button>})}</div>{action&&<button className="primary wide" onClick={action}>{actionText}</button>}</>}</main>}
function AllWords({words,s,open,back}:any){const[q,setQ]=useState(''),[filter,setFilter]=useState('all');const filtered=words.filter((w:Word)=>{const p=s.progress[w.id]||defaultProgress(),match=w.word.toLowerCase().includes(q.toLowerCase())||w.turkish.toLowerCase().includes(q.toLowerCase());if(!match)return false;if(filter==='notStarted')return!s.learned.includes(w.id);if(filter==='learned')return s.learned.includes(w.id);if(filter==='favorites')return s.favorites.includes(w.id);if(filter==='risky')return p.status==='risky';if(filter==='master')return p.status==='master';if(filter==='custom')return!!w.custom;return true});return <main className="page"><button className="back" onClick={back}>← Ana Sayfa</button><div className="title"><h1>Tüm Kelimeler</h1><p>{words.length} kelimelik kütüphane</p></div><input className="search" value={q} onChange={e=>setQ(e.target.value)} placeholder="Kelime veya Türkçe anlam ara..."/><div className="filters">{[['all','Tümü'],['notStarted','Başlanmadı'],['learned','Öğrenildi'],['risky','Riskli'],['master','Master'],['favorites','Favoriler'],['custom','Eklenenler']].map(([v,t])=><button key={v} className={filter===v?'active':''} onClick={()=>setFilter(v)}>{t}</button>)}</div><p>{filtered.length} kelime gösteriliyor</p><div className="list">{filtered.map((w:Word)=>{const p=s.progress[w.id]||defaultProgress();return <button key={w.id} onClick={()=>open(w.id)}><div><b>{w.word}</b><small>{w.turkish}</small></div><span>{s.learned.includes(w.id)?`${p.accuracy}% · ${p.status}`:'Başlanmadı'}</span></button>})}</div></main>}
function WordDetail({w,s,toggle,back}:any){const p=s.progress[w.id]||defaultProgress();return <main className="page"><button className="back" onClick={back}>← Geri</button><WordCard w={w} fav={s.favorites.includes(w.id)} toggle={toggle}/><section className="panel"><h3>İlerleme</h3><p>Görülme: {p.seen} · Doğru: {p.correct} · Yanlış: {p.wrong}</p><p>Accuracy: {p.accuracy}% · Durum: {p.status}</p><div className="accuracyTrack"><i style={{width:`${p.accuracy}%`}}/></div><p>Son görülme: {p.lastSeen||'Henüz yok'} · Sonraki tekrar: {p.nextReview||'Planlanmadı'}</p></section></main>}
function Settings({s,setS,back,custom}:any){const file=useRef<HTMLInputElement>(null);const set=(k:string,v:any)=>setS((x:State)=>({...x,settings:{...x.settings,[k]:v},...(k==='dailyWords'?{todayIds:[],previewIds:[],todayCursor:0,todayCompleted:false,todayChallengeTarget:0,todayChallengeCompleted:0}:{})}));const exportData=()=>{const blob=new Blob([JSON.stringify(s,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`wordlock-backup-${localToday()}.json`;a.click();URL.revokeObjectURL(a.href)};const importData=async(e:React.ChangeEvent<HTMLInputElement>)=>{const f=e.target.files?.[0];if(!f)return;try{const data=JSON.parse(await f.text());if(!validateBackup(data))throw new Error();setS((x:State)=>({...x,...data,version:5,settings:{...x.settings,...data.settings},customWords:Array.isArray(data.customWords)?data.customWords:[],previewIds:Array.isArray(data.previewIds)?data.previewIds:[]}));alert('Backup başarıyla yüklendi.')}catch{alert('Geçersiz WordLock backup dosyası.')}e.target.value=''};return <main className="page"><button className="back" onClick={back}>← Ana Sayfa</button><div className="title"><h1>Öğrenme Planı</h1></div><section className="panel settings"><h2>Günlük Öğrenme</h2><Setting label="Yeni kelime sayısı"><Choices v={s.settings.dailyWords} vals={[3,5,7,10]} set={(v:number)=>set('dailyWords',v)}/></Setting><Setting label="Challenge soru sayısı"><Choices v={s.settings.challengeSize} vals={[10,20,30]} set={(v:number)=>set('challengeSize',v)}/></Setting></section><section className="panel settings"><h2>Challenge Tercihleri</h2><Toggle label="Yanlışta Türkçe anlamı göster" v={s.settings.showTurkishAfterWrong} set={(v:boolean)=>set('showTurkishAfterWrong',v)}/><Toggle label="Yanlışta örnek cümleyi göster" v={s.settings.showExamplesAfterWrong} set={(v:boolean)=>set('showExamplesAfterWrong',v)}/><Toggle label="Favorileri günlük challenge'a dahil et" v={s.settings.includeFavorites} set={(v:boolean)=>set('includeFavorites',v)}/></section><section className="panel settings"><h2>Veriler ve Kelimeler</h2><div className="settingsActions"><button className="secondary" onClick={custom}>+ Kendi Kelimeni Ekle</button><button className="secondary" onClick={exportData}>Backup İndir</button><button className="secondary" onClick={()=>file.current?.click()}>Backup Yükle</button><input ref={file} type="file" accept="application/json" hidden onChange={importData}/></div></section><section className="panel danger"><button onClick={()=>{if(confirm('Tüm ilerleme silinsin mi?')){resetState();location.reload()}}}>Tüm İlerlemeyi Sıfırla</button></section></main>}
function CustomWords({s,setS,back}:any){const[form,setForm]=useState({word:'',pos:'noun',definition:'',turkish:'',synonyms:'',antonyms:'',example:'',fillBlank:''}),change=(k:string,v:string)=>setForm(x=>({...x,[k]:v})),save=()=>{if(!form.word.trim()||!form.definition.trim()||!form.turkish.trim()){alert('Word, definition ve Türkçe alanları zorunludur.');return}const id=Math.max(100000,...s.customWords.map((w:Word)=>w.id))+1,word:Word={id,word:form.word.trim(),pos:form.pos.trim()||'word',definition:form.definition.trim(),turkish:form.turkish.trim(),difficulty:'custom',category:'custom',synonyms:form.synonyms.split(',').map(x=>x.trim()).filter(Boolean),antonyms:form.antonyms.split(',').map(x=>x.trim()).filter(Boolean),examples:[form.example.trim()].filter(Boolean),fillBlank:form.fillBlank.trim()||form.example.trim().replace(new RegExp(form.word.trim(),'i'),'_____'),distractors:[],custom:true};setS((x:State)=>({...x,customWords:[...x.customWords,word]}));setForm({word:'',pos:'noun',definition:'',turkish:'',synonyms:'',antonyms:'',example:'',fillBlank:''})};return <main className="page"><button className="back" onClick={back}>← Ayarlar</button><div className="title"><h1>Kendi Kelimelerin</h1><p>AI bağlantısı yoktur. Word, definition ve Türkçe zorunludur.</p></div><section className="panel customForm">{Object.entries(form).map(([k,v])=><label key={k}><span>{k}</span><input value={v} onChange={e=>change(k,e.target.value)} placeholder={k==='synonyms'||k==='antonyms'?'Virgülle ayır':''}/></label>)}<button className="primary" onClick={save}>Kelimeyi Kaydet</button></section><div className="list">{s.customWords.map((w:Word)=><div className="customRow" key={w.id}><div><b>{w.word}</b><small>{w.turkish}</small></div><button onClick={()=>setS((x:State)=>({...x,customWords:x.customWords.filter(a=>a.id!==w.id),favorites:x.favorites.filter(id=>id!==w.id),learned:x.learned.filter(id=>id!==w.id)}))}>Sil</button></div>)}</div></main>}
const Setting=({label,children}:any)=><div className="setting"><b>{label}</b>{children}</div>;const Choices=({v,vals,set}:any)=><div className="choices">{vals.map((x:number)=><button className={v===x?'selected':''} onClick={()=>set(x)} key={x}>{x}</button>)}</div>;const Toggle=({label,v,set}:any)=><label className="toggle"><span>{label}</span><input type="checkbox" checked={v} onChange={e=>set(e.target.checked)}/></label>;const Nav=({current,go}:any)=><nav><button className={current==='home'?'active':''} onClick={()=>go('home')}>⌂<span>Ana Sayfa</span></button><button className={current==='today'?'active':''} onClick={()=>go('today')}>▣<span>Bugün</span></button><button className={current==='achievements'?'active':''} onClick={()=>go('achievements')}>🏅<span>Başarılar</span></button><button className={current==='settings'?'active':''} onClick={()=>go('settings')}>⚙<span>Ayarlar</span></button></nav>;
createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);
