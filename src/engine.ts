import type {Word} from './types';
function hash(seed:number){let t=seed+0x6D2B79F5;return()=>{t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296}}
export function dailySet(words:Word[],count:number,day:number,used:number[]):Word[]{const pool=words.filter(w=>!used.includes(w.id));const rnd=hash(day*7919+count*101);const scored=pool.map(w=>({w,r:rnd()})).sort((a,b)=>a.r-b.r);const selected:Word[]=[];for(const x of scored){if(selected.length>=count)break;const initial=x.w.word[0].toLowerCase();const sameInitial=selected.filter(s=>s.word[0].toLowerCase()===initial).length;const sameCategory=selected.filter(s=>s.category===x.w.category).length;if(sameInitial===0&&sameCategory<2)selected.push(x.w)}for(const x of scored){if(selected.length>=count)break;if(!selected.some(s=>s.id===x.w.id))selected.push(x.w)}return selected}